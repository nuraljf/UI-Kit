# assets

Drop the showcase background photo here as `background.jpg`, then uncomment the
`background-image` line in `css/showcase.css` (`.stage__bg`).

The original Figma frame uses a stock photo named
`breathtaking-view-forest-covered-with-snow-sunset-norway`. Until a real image
is added, the showcase renders a CSS gradient stand-in that matches its palette.
